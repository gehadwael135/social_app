import 'package:flutter/material.dart';

class UserScree extends StatelessWidget {
  const UserScree({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("users"),),
    );
  }
}